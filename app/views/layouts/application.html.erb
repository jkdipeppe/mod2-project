<!DOCTYPE html>
<html>

  <head>
    <title>Mod2project</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="ui secondary  menu" id="nav_bar">
          <%= link_to "Home", "/", class: "item" %>

          <%= link_to "Shows", shows_path, class: "item" %>


          <%= link_to "Bands", bands_path, class: "item" %>


          <%= link_to "Venues", venues_path, class: "item" %>

          <div class="right menu">
            <div class="item">
              <div class="ui icon input">
                <input type="text" name="name" placeholder="Search band name..." action="/search" method="get">

                <i class="search link icon"></i>
              </div>
            </div>
              <% if session[:user] != nil %>
                <%if session[:user]["bandmates"]%>
                  <%= link_to "Create Show", new_show_path, class: "ui item" %>
                <% end %>
              <% end %>
              <% if session[:user] %>


              <%= link_to session[:user]["name"], (session[:user]["bandmates"] ?
              band_path(session[:user]["id"]): guest_path(session[:user]["id"])), class: "ui item" %>
                <%= link_to "Sign Out", sessions_logout_path, class: "ui item" %></br>
              <% else %>
                <%= link_to "Sign In", sessions_new_path, class: "ui item" %></br>
              <% end %>
          </div>
</div>
    <!-- <div id="site-nav">

    <%= link_to "Sign In", sessions_new_path%>

    <%= link_to "Home", "/" %>
    <%= link_to "Shows", shows_path %>
    <%= link_to "Bands", bands_path %>
    <%= link_to "Venues", venues_path %>
    <%= link_to "Sign Out", sessions_logout_path %></br>
    <% if session[:user] %>
      Logged in as:

      <%= link_to session[:user]["name"], (session[:user]["bandmates"] ?
      band_path(session[:user]["id"]): guest_path(session[:user]["id"])) %>
    <% end %>
    <% if session[:user] != nil %>
      <%if session[:user]["bandmates"]%>
        <%= link_to "Create Show", new_show_path %>
      <% end %>
    <% end %>
  </br>
  </div> -->

    </br></br>
    <%= yield %>

    <div class="ui inverted vertical footer segment">
    <div class="ui container">
      <div class="ui stackable inverted divided equal height stackable grid">
        <div class="three wide column">
          <h4 class="ui inverted header">About</h4>
          <div class="ui inverted link list">
            <a href="#" class="item"><%= link_to "Home", "/", class: "item" %></a>
            <%= link_to "Bands", bands_path, class:"item"%>
            <%= link_to "Shows", shows_path, class:"item"%>
            <%= link_to "Venues", venues_path, class:"item"%>
          </div>
        </div>
        <div class="three wide column">
          <h4 class="ui inverted header">Services</h4>
          <div class="ui inverted link list">
            <a href="#" class="item">Banana Pre-Order</a>
            <a href="#" class="item">DNA FAQ</a>
            <a href="#" class="item">How To Access</a>
            <a href="#" class="item">Favorite X-Men</a>
          </div>
        </div>
        <div class="seven wide column">
          <h4 class="ui inverted header">Footer Header</h4>
          <p>Extra space for a call to action inside the footer that could help re-engage users.</p>
        </div>
      </div>
    </div>
  </div>

  </body>
</html>
